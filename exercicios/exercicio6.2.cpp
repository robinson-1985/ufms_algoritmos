/* 6.2 Escreva um programa que receba três valores, armazenando-os nas variáveis x, y e z, 
e ordene esses valores de modo que, ao final, o menor valor esteja armazenado na variável x,
o valor intermediário esteja armazenado na variável y e o maior valor esteja armazenado
na variável z. */

#include <stdio.h>
int main (){
    int x, y, z;
    printf("Digite três números: ");
    scanf("%d" "%d" "%d", &x, &y, &z);

    



    getchar();
    return 0;
}