/* 7.2 Para transformar um número inteiro i no menor inteiro m maior que i e múltiplo 
de um número inteiro j, a seguinte fórmula pode ser utilizada: m = i + j − i mod j , 
onde o operador mod é o operador de resto de divisão inteira na notação matemática
usual, que corresponde ao operador % na linguagem C.Por exemplo, suponha que usamos 
i = 256 dias para alguma atividade e queremos saberqual o total de dias m que devemos 
ter de forma que esse número seja divisível por j = 7,para termos uma idéia do número 
de semanas que usaremos na atividade. Então, pela fórmula acima, temos que
m = 256 + 7 − 256 mod 7
= 256 + 7 − 4
= 259 

Escreva um programa que receba dois números inteiros positivos i e j e devolva o menor
inteiro m maior que i e múltiplo de j. */


#include <stdio.h>
int main (){



    getchar();
    return 0;
}